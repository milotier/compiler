func :: (foo: u32, bar: char) {
    if foo {
        foo(
            bar + 10,
            &baz ^,
            boop,
        );
    } else
        bar;

    while x < 10  {
        x += 1;
        break;
        continue;
        return x;
    }

    var : u32 = bar + baz;
}
foo :: () { return 10; };
