func :: (foo: u32, bar: i32) {
    if foo {
        foo(
            bar + 10,
            &baz ^,
            boop,
        );
    } else
        bar;

    while x < 10  {
        x += 1;
        break;
        continue;
        return x;
    }

    var : u32 = bar + baz;
}

INT2 :: INT + 1;
INT :: 10 ^ 20;

FLOAT :: 10.0 * 20.0;
BOOL :: true && false;
