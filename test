func :: (foo: u32, bar: i32) {
    if foo {
        foo(
            bar + 10,
            &baz ^,
            boop,
        );
    } else
        bar;

    while x < 10  {
        x += 1;
        break;
        continue;
        return x;
    }

    var : u32 = bar + baz;
}
int : u8 : 10 ^ 20;
float : f32 : 10.0 + 20.0;
boolean : bool : true;
